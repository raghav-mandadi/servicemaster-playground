---
phase: 01-cleanup-data-unification
plan: 03
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified:
  - src/pages/Users.tsx
  - src/pages/Dashboard.tsx
  - src/pages/Templates.tsx
  - src/data/mockData.ts
autonomous: true
requirements:
  - DATA-01
  - DATA-02
  - DATA-03
  - CLEAN-03
  - CLEAN-04

must_haves:
  truths:
    - "Users.tsx renders users from users.json — showing userId-based ids, FieldRole roles (gm, ops_manager, supervisor, cs, sales), and no status column"
    - "Dashboard.tsx imports recentActivity from mockDataLoader.ts, not mockData.ts"
    - "Templates.tsx imports templates from mockDataLoader.ts, not mockData.ts"
    - "No file in src/pages/ or src/components/ imports from src/data/mockData.ts"
    - "mockData.ts is empty (or deleted) — it no longer exports users, templates, or recentActivity"
    - "TypeScript build passes with zero errors"
  artifacts:
    - path: "src/pages/Users.tsx"
      provides: "Users page consuming users.json via mockDataLoader.ts"
      contains: "mockDataLoader"
    - path: "src/pages/Dashboard.tsx"
      provides: "Dashboard importing recentActivity from mockDataLoader.ts"
      contains: "mockDataLoader"
    - path: "src/pages/Templates.tsx"
      provides: "Templates page importing from mockDataLoader.ts"
      contains: "mockDataLoader"
    - path: "src/data/mockData.ts"
      provides: "Retired data file"
  key_links:
    - from: "src/pages/Users.tsx"
      to: "src/data/mockDataLoader.ts"
      via: "import users"
      pattern: "from.*mockDataLoader"
    - from: "src/pages/Dashboard.tsx"
      to: "src/data/mockDataLoader.ts"
      via: "import recentActivity"
      pattern: "from.*mockDataLoader"
---

<objective>
Migrate the three remaining pages off mockData.ts, update Users.tsx to handle the new User schema from users.json, and retire mockData.ts entirely.

Purpose: This is the final step that eliminates all parallel data sources. After this plan, every page imports exclusively from mockDataLoader.ts and the old mockData.ts is gone.
Output: Three updated pages all importing from mockDataLoader.ts; mockData.ts removed.
</objective>

<execution_context>
@/Users/raghavmandadi/Desktop/leonwork/servicemaster/.claude/get-shit-done/workflows/execute-plan.md
@/Users/raghavmandadi/Desktop/leonwork/servicemaster/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-cleanup-data-unification/01-02-SUMMARY.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate Users.tsx to mockDataLoader.ts and update for new User schema</name>
  <files>src/pages/Users.tsx</files>

  <read_first>
    - src/pages/Users.tsx — full current file (read entirely before editing)
    - src/data/mockDataLoader.ts — confirm `users` export exists and its type
    - src/types/index.ts — User interface after Plan 01 (fields: userId, name, email, role as FieldRole, assignedSiteIds)
    - src/data/users.json — actual data to verify what will render
  </read_first>

  <action>
Make the following changes to src/pages/Users.tsx:

**1. Replace import line**
Change: `import { users } from '../data/mockData';`
To: `import { users } from '../data/mockDataLoader';`

**2. Update the user row key**
Change: `{users.map(user => (` `<div key={user.id}` ...`)`
The new User schema uses `userId` not `id`. Update to `key={user.userId}`.

**3. Update Role cell**
The old code renders: `<Tag>{user.role}</Tag>` — the role was 'Admin', 'Manager', 'Rep'.
The new roles are 'gm', 'ops_manager', 'supervisor', 'cs', 'sales'.

Add a role display map at the top of the component (inside the function, before the return):
```typescript
const ROLE_LABEL: Record<string, string> = {
  gm:          'GM',
  ops_manager: 'Ops Manager',
  supervisor:  'Supervisor',
  cs:          'CS',
  sales:       'Sales',
};
```

Update the role cell: `<Tag>{ROLE_LABEL[user.role] ?? user.role}</Tag>`

**4. Remove Status column**
The new User type has no `status` field. Remove:
- 'Status' from the HEADERS array (currently: `['Name', 'Role', 'Status', 'Last Login', 'Actions']`)
- The `<StatusChip status={user.status} />` cell from the row
- The `StatusChip` import (`import { StatusChip } from '../components/ui/StatusChip';`) — only remove if StatusChip is not used elsewhere in this file

**5. Remove Last Login column**
The new User type has no `lastLogin` field. Remove:
- 'Last Login' from the HEADERS array
- The `{user.lastLogin}` cell from the row

Updated HEADERS should be: `['Name', 'Role', 'Actions']` with appropriate flex widths (Name: flex 1, Role: flex 1, Actions: fixed 100px).

**6. Fix getInitials helper**
The function is already correct — it uses `user.name` which still exists. No change needed.

After edits, verify `npx tsc --noEmit` shows no errors in this file.
  </action>

  <verify>
    <automated>grep -n "from.*mockDataLoader\|user\.userId\|ROLE_LABEL\|assignedSiteIds" /Users/raghavmandadi/Desktop/leonwork/servicemaster/src/pages/Users.tsx</automated>
  </verify>

  <acceptance_criteria>
    - `grep "from.*mockData'" src/pages/Users.tsx` returns no results (old import gone)
    - `grep "from.*mockDataLoader" src/pages/Users.tsx` returns the import line
    - `grep "user\.id\b" src/pages/Users.tsx` returns no results (old id field gone)
    - `grep "user\.userId" src/pages/Users.tsx` returns the key={user.userId} usage
    - `grep "user\.status\|user\.lastLogin" src/pages/Users.tsx` returns no results (fields removed)
    - `grep "ROLE_LABEL" src/pages/Users.tsx` returns the map definition and its usage
    - `grep "Last Login\|Status" src/pages/Users.tsx` returns no results (columns removed from headers)
  </acceptance_criteria>

  <done>Users.tsx imports from mockDataLoader.ts; renders 10 users from users.json; shows Name, Role, Actions columns; role displayed as human label (GM, Ops Manager, etc.); no status or lastLogin column.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate Dashboard.tsx and Templates.tsx; retire mockData.ts</name>
  <files>src/pages/Dashboard.tsx, src/pages/Templates.tsx, src/data/mockData.ts</files>

  <read_first>
    - src/pages/Dashboard.tsx — full current file
    - src/pages/Templates.tsx — full current file
    - src/data/mockDataLoader.ts — confirm recentActivity and templates exports exist
    - src/data/mockData.ts — full current file (to understand everything it exports before deleting)
  </read_first>

  <action>
**Dashboard.tsx changes:**

Change: `import { recentActivity } from '../data/mockData';`
To: `import { recentActivity } from '../data/mockDataLoader';`

No other changes needed — the recentActivity shape (id, text, time) is identical between mockData.ts and the new mockDataLoader.ts export.

**Templates.tsx changes:**

Change: `import { templates } from '../data/mockData';`
To: `import { templates } from '../data/mockDataLoader';`

No other changes needed — the Template shape is identical (same data migrated in Plan 02 Task 2).

**Retire mockData.ts:**

After migrating all imports away from mockData.ts, verify no files still import from it:
```bash
grep -rn "from.*mockData'" src/
```
Expected result: no output (zero imports remaining).

Once confirmed, delete the file:
```bash
rm src/data/mockData.ts
```

If any unexpected import is found during the grep check, fix that import to use mockDataLoader.ts before deleting.

Run `npx tsc --noEmit` after deletion to confirm no compilation errors. The build must be clean.

Run `npm run build` to confirm production build succeeds (this does tsc + vite build).
  </action>

  <verify>
    <automated>grep -rn "from.*['\"]../data/mockData['\"]" /Users/raghavmandadi/Desktop/leonwork/servicemaster/src/ && echo "FAIL: mockData imports remain" || echo "PASS: no mockData imports"</automated>
  </verify>

  <acceptance_criteria>
    - `grep "from.*mockData'" src/pages/Dashboard.tsx` returns no results
    - `grep "from.*mockDataLoader" src/pages/Dashboard.tsx` returns the import line
    - `grep "from.*mockData'" src/pages/Templates.tsx` returns no results
    - `grep "from.*mockDataLoader" src/pages/Templates.tsx` returns the import line
    - `ls src/data/mockData.ts 2>&1` returns "No such file" (file deleted)
    - `grep -rn "from.*mockData'" src/` returns no results (no surviving imports)
    - `npx tsc --noEmit` exits with code 0 (zero TypeScript errors)
  </acceptance_criteria>

  <done>Dashboard.tsx and Templates.tsx import from mockDataLoader.ts. mockData.ts deleted. Zero imports to mockData.ts remain anywhere in src/. TypeScript build is clean.</done>
</task>

</tasks>

<verification>
Final phase verification — run all of these after Plan 03 completes:

1. `grep -rn "from.*mockData'" src/` — must return zero results
2. `grep -rn "from.*mockDataLoader" src/pages/` — must return at least one result per file for Users.tsx, Dashboard.tsx, Templates.tsx, Accounts.tsx, Health.tsx
3. `grep -rn "PreJobSurveyResponse\|PostJobSurveyResponse\|recentSurveys\|preJobSurveys" src/` — must return zero results
4. `grep "FieldRole\|userId.*string" src/types/index.ts` — both present
5. `grep "watchlist\|incidentCount12m" src/data/mockDataLoader.ts` — present (riskProfile derivation)
6. `npx tsc --noEmit` — exit code 0
7. `npm run build` — exit code 0
</verification>

<success_criteria>
- All pages (Accounts, Health, Dashboard, Users, Templates) import exclusively from mockDataLoader.ts — no other data file imports
- Users page shows 10 users from users.json with FieldRole display names (GM, Ops Manager, Supervisor, CS, Sales)
- mockData.ts deleted; no surviving imports to it
- TypeScript compiles clean; production build succeeds
- riskProfile in AccountHealthScore derived from events, not hardcoded JSON
- No survey type references anywhere in src/
</success_criteria>

<output>
After completion, create `.planning/phases/01-cleanup-data-unification/01-03-SUMMARY.md`
</output>
