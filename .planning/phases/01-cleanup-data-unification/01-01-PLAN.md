---
phase: 01-cleanup-data-unification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/index.ts
  - src/types/health.ts
autonomous: true
requirements:
  - DATA-03
  - CLEAN-01
  - CLEAN-02

must_haves:
  truths:
    - "The User type in src/types/index.ts matches the users.json schema exactly: userId, name, role, email, assignedSiteIds with no status or lastLogin fields"
    - "src/types/health.ts contains no definitions or exports for PreJobSurveyResponse, PostJobSurveyResponse, CriterionRating, PostJobCriterionRating"
    - "AccountHealthScore interface has no recentSurveys or preJobSurveys fields"
    - "TypeScript build passes with no errors after changes"
  artifacts:
    - path: "src/types/index.ts"
      provides: "Canonical User type matching users.json schema"
      contains: "type FieldRole"
    - path: "src/types/health.ts"
      provides: "Health types with survey interfaces removed"
  key_links:
    - from: "src/types/index.ts"
      to: "src/data/users.json"
      via: "User interface field names match JSON keys"
      pattern: "userId.*string"
    - from: "src/types/health.ts"
      to: "AccountHealthScore"
      via: "interface definition"
      pattern: "recentSurveys|preJobSurveys"
---

<objective>
Update the two type definition files to establish the correct contracts before runtime code is touched.

Purpose: Every downstream change in Plans 02 and 03 depends on having the correct User shape and a clean health type file. Doing types first means the TypeScript compiler validates correctness automatically.
Output: Updated src/types/index.ts with users.json-compatible User type; src/types/health.ts with survey interfaces removed.
</objective>

<execution_context>
@/Users/raghavmandadi/Desktop/leonwork/servicemaster/.claude/get-shit-done/workflows/execute-plan.md
@/Users/raghavmandadi/Desktop/leonwork/servicemaster/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace User type in src/types/index.ts with users.json-compatible schema</name>
  <files>src/types/index.ts</files>

  <read_first>
    - src/types/index.ts — current file being replaced
    - src/data/users.json — canonical schema to match
    - src/pages/Health.tsx — to see how users.json fields are already consumed (role, userId, assignedSiteIds)
    - src/data/mockData.ts — see old User usage to understand what's being retired
  </read_first>

  <action>
Replace the User-related type declarations in src/types/index.ts. The current file defines:
```typescript
export type UserRole = 'Admin' | 'Manager' | 'Rep';
export type UserStatus = 'Active' | 'Inactive';
export interface User {
  id: string;
  name: string;
  email: string;
  role: UserRole;
  status: UserStatus;
  lastLogin: string;
}
```

Replace these with:
```typescript
export type FieldRole = 'gm' | 'ops_manager' | 'supervisor' | 'cs' | 'sales';

export interface User {
  userId: string;
  name: string;
  email: string;
  role: FieldRole;
  assignedSiteIds: string[];
}
```

Keep ALL other existing declarations in the file unchanged: AccountStatus, DealStatus, UserStatus (can be removed — it was only used with the old User.status field), CleaningFrequency, PricingType, RoomType, Room, BuildingFloor, BuildingDetail, Account, Template.

Remove: `UserRole = 'Admin' | 'Manager' | 'Rep'` and `UserStatus = 'Active' | 'Inactive'` — these are no longer used after migrating to the users.json schema. Also remove `status: UserStatus` and `lastLogin: string` from the User interface (they don't exist in users.json).

After edits, run `npm run build` (via `npx tsc --noEmit`) to check for type errors. Expected: errors in src/data/mockData.ts (uses old User shape) and src/pages/Users.tsx (uses old User fields like user.status, user.lastLogin, user.role as UserRole). These will be fixed in Plan 03 — note them but do not fix yet in this plan.
  </action>

  <verify>
    <automated>grep -n "userId.*string\|FieldRole\|assignedSiteIds" /Users/raghavmandadi/Desktop/leonwork/servicemaster/src/types/index.ts</automated>
  </verify>

  <acceptance_criteria>
    - `grep "UserRole" src/types/index.ts` returns no results (type removed)
    - `grep "FieldRole" src/types/index.ts` returns `export type FieldRole = 'gm' | 'ops_manager' | 'supervisor' | 'cs' | 'sales';`
    - `grep "userId" src/types/index.ts` returns `  userId: string;` inside the User interface
    - `grep "assignedSiteIds" src/types/index.ts` returns `  assignedSiteIds: string[];`
    - `grep "status.*UserStatus\|lastLogin" src/types/index.ts` returns no results (fields removed from User interface)
  </acceptance_criteria>

  <done>src/types/index.ts User interface matches users.json schema: userId, name, email, role (FieldRole), assignedSiteIds. Old UserRole and status/lastLogin fields gone.</done>
</task>

<task type="auto">
  <name>Task 2: Remove survey types and fields from src/types/health.ts</name>
  <files>src/types/health.ts</files>

  <read_first>
    - src/types/health.ts — full file being edited (read entirely before touching)
  </read_first>

  <action>
Remove the following from src/types/health.ts:

1. Remove `SurveyCriterion` type (lines 3-13): the type union of 'restrooms' | 'lobby' | ... | 'communication'

2. Remove `CriterionRating` type (line 15): `export type CriterionRating = 1 | 2 | 3 | 4 | 5;`

3. Remove `PreJobSurveyResponse` interface (lines 59-66): entire interface block

4. Remove `PostJobCriterionRating` interface (lines 68-72): entire interface block

5. Remove `PostJobSurveyResponse` interface (lines 74-85): entire interface block

6. Remove these two fields from the `AccountHealthScore` interface (lines 197-198):
   ```typescript
   recentSurveys?: PostJobSurveyResponse[];
   preJobSurveys?: PreJobSurveyResponse[];
   ```

Keep EVERYTHING else in the file unchanged:
- CriterionMeta interface (used by CRITERIA_META / EXTRA_CRITERIA_META — still consumed by active mobile screens EventLogScreen.tsx)
- CRITERIA_META array
- EXTRA_CRITERIA_META array
- ALL_PRESET_META
- getCriterionMeta function
- All HealthEvent, HealthSignal, RiskProfile, AccountHealthScore, SiteContact types
- All tier helper functions (getTierFromScore, getTierLabel, etc.)
- TIER_REQUIREMENTS constant

IMPORTANT: CriterionMeta and CRITERIA_META are retained because they are used by active mobile screens (EventLogScreen.tsx in the current mobile app). Only the survey *flow* types (PreJobSurveyResponse, PostJobSurveyResponse, CriterionRating) are removed.

Run `npx tsc --noEmit` after changes. If there are errors referencing the removed types, note them — they indicate dead import sites that also need cleanup.
  </action>

  <verify>
    <automated>grep -n "PreJobSurveyResponse\|PostJobSurveyResponse\|CriterionRating\|recentSurveys\|preJobSurveys" /Users/raghavmandadi/Desktop/leonwork/servicemaster/src/types/health.ts</automated>
  </verify>

  <acceptance_criteria>
    - `grep "PreJobSurveyResponse" src/types/health.ts` returns no results
    - `grep "PostJobSurveyResponse" src/types/health.ts` returns no results
    - `grep "CriterionRating" src/types/health.ts` returns no results (the type alias, not PostJobCriterionRating which is also removed)
    - `grep "recentSurveys\|preJobSurveys" src/types/health.ts` returns no results
    - `grep "CRITERIA_META\|CriterionMeta" src/types/health.ts` still returns results (these are kept)
    - `grep "AccountHealthScore" src/types/health.ts` still returns the interface definition (kept)
  </acceptance_criteria>

  <done>Survey flow types (PreJobSurveyResponse, PostJobSurveyResponse, CriterionRating, PostJobCriterionRating) and their AccountHealthScore fields (recentSurveys, preJobSurveys) are removed. CriterionMeta and CRITERIA_META constants remain.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `grep -rn "PreJobSurveyResponse\|PostJobSurveyResponse\|SurveyCriterion" src/` — should return no results anywhere in src/
2. `grep -n "FieldRole\|userId.*string" src/types/index.ts` — both lines must be present
3. `grep -n "UserRole\b" src/types/index.ts` — must return no results
4. `npx tsc --noEmit` — errors are expected only in mockData.ts and Users.tsx (old User schema users), NOT in health.ts or any mobile screen component
</verification>

<success_criteria>
- src/types/index.ts User interface has: userId, name, email, role (FieldRole), assignedSiteIds — matches users.json exactly
- src/types/health.ts has no PreJobSurveyResponse, PostJobSurveyResponse, CriterionRating exports
- AccountHealthScore has no recentSurveys or preJobSurveys fields
- CRITERIA_META and CriterionMeta are still present (used by active mobile screens)
</success_criteria>

<output>
After completion, create `.planning/phases/01-cleanup-data-unification/01-01-SUMMARY.md`
</output>
